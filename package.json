{
  "name": "kafka-node",
  "version": "1.0.0",
  "description": "docker-compose -f docker-compose.yml up -d",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kafka-node": "^5.0.0"
  }
}
